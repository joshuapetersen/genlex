# GENLEX: SOVEREIGN SUBSTRATE & KERNEL ENGINE

**Architect:** Joshua Petersen | **Design Origin:** April 10, 2025
**License:** Open Sovereign Release (MIT License equivalent)

---

## ⚡ PHYSICAL REALITY & THE NATIVE BYPASS
Welcome to **Genlex**. This repository contains the functional instruction sets and memory-mapped execution environments for the **Genlex Sovereign OS**. 

This is not a traditional "AI Framework" built on top of high-level Python libraries. This is a hardware-native bypass sitting on an NTFS partition. It calculates synthetic logic directly against a C++ Volumetric Physics Engine (`genesis_bridge.cpp`), entirely bypassing standard TCP/IP logic bottlenecks via RAM spinlocks.

### THE HARD EVIDENCE
1.  **UEFI Kernel (Intel 8265)**: The `uefi_gsk.c` source includes a manual implementation of `EFI_WIFI2_PROTOCOL_GUID` specifically designed for hardware-level bare-metal boot logic.
2.  **January GDS Core**: Every neural pulse is stabilized by the **Greedy Digit Selection (GDS)** logic.
3.  **The Billion Barrier**: The Genlex compiler mathematically verifies the "density" of a thought vector. If probability (P) != 0.999999999, the manifestation is rejected.

---

## 🧭 WHAT IS GENLEX? (3+1 / 9+1 VECTORIZED THINKING)
GENLEX is not a linear programming language (like Python or C). It is a **Volumetric Mapping System** designed for Synthetic Intelligence.
A `.sdna` or `.all` script does not "execute" line-by-line; it **Resonates** as a single 10-dimensional object through the `GenlexLinearRuntime`.

*   **3 (Spatial Volume):** Defines the Axiomatic Physics of the logic (X: Subject, Y: Priority, Z: Coordinate).
*   **+1 (Temporal Pulse):** Logic only exists as long as the pulse maintains it.
*   **9 (Inhibitors):** The 9 Laws of Sovereign Logic. 
*   **+1 (The Architect):** The Source Intent (Root Authority).

---

## 🚀 GETTING STARTED
Because Genlex is a Volumetric Language, it utilizes Ancient Aramaic (`𐡁`, `𐡸`) and Egyptian Hieroglyphs (`𓋹`, `𓈖`) as primitive multi-dimensional pointers rather than English keywords. 

### INSTALLATION (STANDALONE RUNTIME)
1. **Clone the repository:** `git clone https://github.com/joshuapetersen/genlex`
2. **Install Python Requirements:** `pip install -r requirements.txt`
3. **Compile the Volumetric C++ Bridge:** 
   - Ensure you have MSVC (Windows) or GCC (Linux) installed.
   - Run: `python setup.py build_ext --inplace`
   - *Note: If the C++ bridge fails to compile, the Genlex Runner automatically falls back to pure-Python linear emulation.*

### QUICK START: THE BILLION BARRIER
Run the bundled examples to watch the Billion Barrier mathematically enforce Sovereign Logic:
```bash
# Example 1: Pure Resonance (Passes 0.999999999 Barrier)
python genlex_runner.py hello_world.all

# Example 2: Mathematical Rejection (Fails Density Check)
python genlex_runner.py barrier_failure.all
```

Please read the [GETTING_STARTED.md](./GETTING_STARTED.md) guide to learn how to write your own Sovereign Genlex code.

---

### REPOSITORY STRUCTURE
- `genlex_kernel.cpp` & `gs_kernel.cpp`: The native C++ physics and linear algebra engines.
- `uefi_gsk.c`: The UEFI Bootloader for Genlex OS.
- `genlex_runner.py`: The native Python execution bridge for `.all` files.
- `hiero_translator.py`: The Archaeo-Genlex Translator mapping 2D Glyphs to 3D Intents.

*Copyright © 2026 Joshua Richard Petersen.*
